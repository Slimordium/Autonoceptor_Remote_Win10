<Page
    x:Class="Autonoceptor.Host.Views.ShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Autonoceptor.Host.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Width="Auto" Height="Auto">
        <Grid.ColumnDefinitions>
            <ColumnDefinition MinWidth="110" Width=".5*" />
            <ColumnDefinition MinWidth="150" Width="1.5*" />
            <ColumnDefinition Width=".8*" />
            <ColumnDefinition Width="1.5*" />
            <ColumnDefinition Width="1.5*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="70" />
            <RowDefinition Height="70" />
            <RowDefinition Height="70" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Broker IP" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"></TextBlock>
        <TextBox Name="BrokerIp" Grid.Row="0" Grid.Column="1" Height="28"></TextBox>
        <Button Name="StartConductor" Grid.Column="2" Grid.Row="0" Content="Init Hw" HorizontalAlignment="Center"></Button>

        <ScrollViewer Grid.Row="0" Grid.Column="3" Grid.RowSpan="4" Grid.ColumnSpan="2" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible" Width="{Binding Path=Width, ElementName=LogColumn}" Margin="3.4,0,0,-0.4" Background="White">
            <ListBox x:Name="LogBox" FontSize="13" FontFamily="Arial Narrow" Background="White" ItemsSource="{Binding Log}" BorderThickness="0" SelectionMode="Multiple" Margin="5,0,0,0" >
                
            </ListBox>
        </ScrollViewer>


        <CaptureElement Grid.Row="3" Grid.Column="0" Name="CaptureElement"></CaptureElement>
    </Grid>
</Page>
