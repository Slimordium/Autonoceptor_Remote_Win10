<Page
    x:Class="Autonoceptor.Host.Views.ShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Autonoceptor.Host.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Width="Auto" Height="Auto">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition Width="460" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="40" />
            <RowDefinition Height="40" />
            <RowDefinition Height="40" />
            <!--<RowDefinition Height="40" />
            <RowDefinition Height="40" />-->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button Name="GetYpr" Grid.Column="3" Grid.Row="0" Content="Get YPR" HorizontalAlignment="Center" Width="200"></Button>
        <TextBox Name="Yaw" Text="0" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center"></TextBox>

        <TextBlock Text="Broker IP" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"></TextBlock>
        <TextBox Name="BrokerIp" Grid.Row="0" Grid.Column="1" Height="28"></TextBox>
        <Button Name="InitializeMqtt" Grid.Column="2" Grid.Row="0" Content="Init MQTT" HorizontalAlignment="Center"></Button>

        <Button Name="GetOdometerData" Grid.Column="3" Grid.Row="2" Content="Get Odometer data" HorizontalAlignment="Center" Width="200"></Button>
        <Button Name="SetGpsNavSpeed" Grid.Column="2" Grid.Row="1" Content="Init GPS" HorizontalAlignment="Center"></Button>

        <Button Name="GetCurrentPosition" Grid.Column="3" Grid.Row="1" Content="Get Current Position" HorizontalAlignment="Center" Width="200"></Button>
        <Button Name="CalibrateImu" Grid.Column="4" Grid.Row="1" Content="Calibrate IMU/GPS Heading" HorizontalAlignment="Center" Width="200"></Button>

        <ComboBox Name="WaypointsCombo" Grid.Column="4" Grid.Row="2" 
                  ItemsSource="{Binding Waypoints}" 
                  SelectedIndex="{Binding SelectedWaypoint}"
                  HorizontalAlignment="Center" Width="450" >
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding GpsFixData}" Width="210"/>
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <Button Name="SetWpBoundry" Grid.Column="2" Grid.Row="2" Content="Dispose GPS" HorizontalAlignment="Center"></Button>

        <Button Name="ListWaypoints" Grid.Column="3" Grid.Row="3" Content="List Waypoints" HorizontalAlignment="Center" Width="200"></Button>
        <Button Name="GetHeadingDistanceToSelected" Grid.Column="4" Grid.Row="3" Content="Get Head/Dist to next WP" HorizontalAlignment="Center" Width="200"></Button>

        <ScrollViewer Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="5" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible" Width="{Binding Path=Width, ElementName=LogColumn}" Margin="3.4,0,0,-0.4">
            <ListBox x:Name="LogBox" FontSize="13" FontFamily="Arial Narrow" ItemsSource="{Binding Log}" BorderThickness="0" SelectionMode="Multiple" Margin="5,0,0,0" >
            </ListBox>
        </ScrollViewer>

    </Grid>
</Page>
